
<%= simple_form_for(discuss_group) do |f| %>
	<div class="form-group">
		<%= f.input :title, label: "標題", input_html: { class: "form-control"} %>
		<%= f.input :description, label: "內容", input_html: { class: "form-control"} %>
		<%= f.submit "送出", disable_with: "submiting...", class: "btn btn-primary" %>
	</div>

<% end %>